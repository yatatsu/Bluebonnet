machine:
  ruby:
    version: 1.9.3-p448
  xcode:
    version: 6.3.1
  environment:
    LC_CTYPE: en_US.UTF-8

dependencies:
  override:
    - sudo gem install cocoapods --no-ri --no-rdoc
  post:
    - make setup

test:
  override:
    - pod spec lint
    - make ci