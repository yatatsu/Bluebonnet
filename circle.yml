machine:
  xcode:
    version: "6.3.1"
  environment:
    LC_CTYPE: en_US.UTF-8

dependencies:
  post:
    - sudo gem install cocoapods xcpretty --no-ri --no-rdoc
    - make setup

test:
  override:
    - pod spec lint
    - make ci